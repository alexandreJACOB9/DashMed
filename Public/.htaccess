RewriteEngine On

# Si fichier ou dossier existe: servir direct
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Tout le reste vers index.php (front controller)
RewriteRule ^ index.php [L]